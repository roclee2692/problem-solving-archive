#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAX =73;
int t;
int main(){
ios::sync_with_stdio(0);cin.tie(0);
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        if(n%4==0){
            cout<<"Bob\n";
        }
        else{
            cout<<"Alice\n";
        }
        // Solve the game for this test case
    }
    return 0;
}
