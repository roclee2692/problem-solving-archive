 /*C++ 中负数取模的表现：
在 C++ 中：
-1 % 5 == -1
也就是说，负数取模结果仍是负数！

这和数学上的“模运算”定义不一样。数学里我们期望结果是 非负数，即：

(-1 mod 5) = 4
但 C++ 会保留负号！

所以为什么不能直接 (rear - 1) % k？
因为一旦 rear == 0，你就

(rear - 1) % k == -1 % k == -1
你再去访问 data[-1] 就越界了，直接崩溃（或者未定义行为）！*/
